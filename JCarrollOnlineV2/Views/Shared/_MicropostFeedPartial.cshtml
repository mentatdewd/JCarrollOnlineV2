@using JCarrollOnlineV2.ViewModels
@using JCarrollOnlineV2.Entities

@model HomeViewModel

@if (Model.Microposts != null)
{
    foreach (Micropost micropost in Model.Microposts)
    {
        @Html.Partial("_MicropostFeedItemPartial", micropost)
    }
}
