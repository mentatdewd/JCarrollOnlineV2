@using JCarrollOnlineV2.ViewModels.Users
@using JCarrollOnlineV2.HtmlHelpers
@model UserStatsViewModel

<div class="stats">
    @Html.MultilineActionLink(Model.UsersFollowing.Users.Count() + "<br/>following", "Details", "Users", new { userId = Model.User.Id }, null)
    @Html.MultilineActionLink(Model.UserFollowers.Users.Count() + "<br/>followers", "Details", "Users", new { userId = Model.User.Id }, null)
</div>
